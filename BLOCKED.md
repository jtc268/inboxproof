# BLOCKED

- Git push: RESOLVED in round 319. Both repos (jtc268/inboxproof and jtc268/inboxproof-cli) were stuck because GitHub Push Protection (GH013) rejected commits containing hardcoded secrets (Resend key in main server.mjs; GitHub PAT in CLI set-topics.mjs). Removed both secrets (env-only), gitignored the secret-bearing dev scripts, rewrote the affected commits, and pushed: main is at a74bf6c, CLI at 8c5184d, both in sync with origin. Verified via pickaxe that no secret remains in either origin history. No longer a blocker.
- Google Search Console: verify inboxproof.email (the brand domain is now live; the old vercel.app host 301s to it) and submit /sitemap.xml. Needs the human's Google account. Until then the site relies on IndexNow (Bing/Yandex) only. Bing IndexNow for the new domain returned 403 SiteVerificationNotCompleted on the second chunk, so Bing verification is also needed for the brand domain. As of round 165 all three IndexNow providers (api.indexnow.org, bing, yandex) still return 403 ("User is unauthorized to access the site" / "Invalid key") even though /key.txt returns 200 with the correct key and the request key matches the key file exactly. Yandex acceptance is intermittent: it returned 202 success for all 124 URLs in round 186 (and again in round 185), but 403 "Invalid key" in other rounds. This is a provider-side cooldown, not a config regression. Re-fire fire-indexnow.mjs every few rounds to catch the windows where Yandex accepts; Google and Bing stay 403 until GSC/Bing verification.
- Launch: fire the Show HN + Product Hunt + directory posts from LAUNCH.md. Needs the human to post under a handle (not the owner's name). All copy is drafted and ready. Links should point at https://inboxproof.email (the vercel.app host 301s there).
- Follow-up email: RESOLVED in round 312. The post-audit follow-up (AUDIT_FOLLOWUP_EMAIL) is now live in production. Set the Vercel env vars (RESEND_API_KEY = the full-access key re_6cmTcjW4..., ALERT_FROM = onboarding@adorellc.pro which is a verified Resend domain, AUDIT_FOLLOWUP_EMAIL=1) and updated the code fallback to match. Verified end-to-end: a fresh free audit for a domain with failing checks returned 200 and the Vercel log shows "[alert] sent to followup-verify-1@resend.dev <resend-id>". Free users who run an audit with failing/warning checks now get an automated follow-up email (7-day cooldown per lead). inboxproof.email is still unverified in Resend (domain limit), so ALERT_FROM uses adorellc.pro; to send from inboxproof.email a human must verify that domain in the Resend dashboard.
- npm publish: inboxproof-cli is not on npm (404). Needs an npm account + token (or .npmrc / NPM_TOKEN) to run `npm publish`. The package is publish-ready (clean package.json, README, MIT, Node 18+, no deps).
- GitHub topics on the CLI repo: the API PAT is read-only for writes, so adding repo topics (e.g. `email-deliverability`, `dmarc`, `spf`) needs a repo-scoped token.
- CLI CI workflow: GitHub refuses to let the OAuth App push a new `.github/workflows/ci.yml` without the `workflow` scope ("refusing to allow an OAuth App to create or update workflow ... without `workflow` scope"). The workflow file is staged locally at inboxproof-cli/.github/workflows/ci.yml (runs the audit in text, JSON, and threshold-gate modes) but cannot be pushed with the current PAT. Needs a human with a repo-scoped token that includes the `workflow` scope to `git push` it.
- CLI repo: CORRECTED in round 319. github.com/jtc268/inboxproof-cli is already PUBLIC (confirmed via GitHub API, private:false), so the earlier "repo is private" note was stale. The README backlink to https://inboxproof.email is publicly visible and `npx github:jtc268/inboxproof-cli` works for the public. The domain-arg parsing fix is now pushed (8c5184d). Remaining gap: the repo has no GitHub topics (discoverability) and is not on npm (see npm publish above).
- Pricing: RESOLVED. Round 261 fixed the bait-and-switch by matching copy to the then-charge ($29/$99). On 2026-08-22 (round 305) I activated the 50% launch discount: the Vercel env API returns 403 so I could not point STRIPE_PRICE_PRO/AGENCY at the launch IDs, instead I hardcoded the launch price IDs (Pro price_1U6dWBFzAAOxCQiQs2LmWAT9 = $14.50, Agency price_1U6dWBFzAAOxCQiQ1rKWK7nU = $49.50) as the source of truth in server.mjs and updated all copy from $29/$99 to $14.50/$49.50. Deployed and verified live: homepage shows $14.50/$49.50 and real Stripe checkout sessions for both plans resolve to the launch price IDs (1450 / 4950 cents). Copy now matches the charge at the launch price. To restore the regular $29/$99 later, swap the two hardcoded IDs in server.mjs back to the full-price IDs (Pro price_1U5yDhFzAAOxCQiQK8mPnm3m, Agency price_1U5yDiFzAAOxCQiQD55iOFao) and revert the copy.
- Brand name collision: "InboxProof" is already a WordPress plugin (wordpress.org/plugins/inboxproof), published June 2026, fewer than 10 active installs, no reviews, by "tazrinhossain". It is a direct competitor (missed-lead detection + SPF/DKIM/DMARC checks + from-domain mismatch for WordPress forms, works alongside SMTP plugins). The exact brand term is taken, so we will not automatically rank for "InboxProof" searches and the brand is not unique. The competing product is tiny (fewer than 10 installs), so we have a realistic chance to outrank it over time, but the human should decide: (a) keep the name and lean into differentiation (SaaS audit vs WP plugin), (b) rename the product, or (c) register a distinct domain. Not something I can fix unilaterally.
